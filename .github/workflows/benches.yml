name: Benchmarks

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  # Include the PR number / branch ref so concurrent PRs don't cancel
  # each other, but pushes to the same ref still cancel in-progress runs.
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  benchmarks:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: sedimentree_core
            package: sedimentree_core
            features: std
            bench: sedimentree
          - name: subduction_core
            package: subduction_core
            features: std
            bench: subduction
          - name: e2e-websocket
            package: subduction_websocket
            features: tokio_client,tokio_server
            bench: e2e
            # Slim sweep: drop 256 KiB / 1 MiB blobs, 4 / 8 concurrent
            # clients, 100-commit batches. Avoids OOM-kill of the
            # GitHub-hosted runner. See subduction_websocket/benches/e2e.rs.
            ci_slim: "1"

    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          # Defer caching to Swatinem/rust-cache so we can use a
          # per-package `shared-key`. Each matrix job keeps its own
          # cache entry keyed by the bench package, which avoids
          # cross-suite invalidation when a single crate changes.
          cache: false

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "bench-${{ matrix.package }}"
          cache-all-crates: true

      - name: Run ${{ matrix.name }} benchmarks
        env:
          # Bench files (currently only e2e.rs) read this to decide
          # whether to run the slim sweep. Empty string = full sweep.
          SUBDUCTION_BENCH_CI_SLIM: ${{ matrix.ci_slim || '' }}
        run: |
          if [ -n "${{ matrix.features }}" ]; then
            cargo bench \
              --package ${{ matrix.package }} \
              --features ${{ matrix.features }} \
              --bench ${{ matrix.bench }} \
              -- --noplot
          else
            cargo bench \
              --package ${{ matrix.package }} \
              --bench ${{ matrix.bench }} \
              -- --noplot
          fi

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results-${{ matrix.name }}
          path: target/criterion/
          retention-days: 90

  heap-profile:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: false

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "heap-profile"

      - name: Run heap allocation profiling
        run: cargo test --package sedimentree_core --test heap_profile -- --nocapture

      - name: Upload heap profile
        uses: actions/upload-artifact@v4
        with:
          name: heap-profile
          path: sedimentree_core/dhat-heap.json
          retention-days: 90
